import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["nainish-paper-ai.vercel.app", "lh3.googleusercontent.com","res.cloudinary.com"],
  },
};

export default nextConfig;
